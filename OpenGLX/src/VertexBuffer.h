#pragma once

class VertexBuffer
{
public:
	void GenerateBuffer(unsigned int& id, const float* data, int size, int type);
	void GenerateBufferi(unsigned int& id, const int* data, int size, int type);
private:
};

